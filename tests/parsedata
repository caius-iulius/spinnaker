use Core

data ParseResult coord t
    = POk t (coord, List Int)
    | PFail coord (List Int)
    | PFatal coord (List Int)

data Parser coord t
    = PValue t
    | PParse ((coord, (List Int)) -> ParseResult coord t)

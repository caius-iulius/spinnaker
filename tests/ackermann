use Core

def ackermann = \mn ->
    put mn
    | (0, n) -> n + 1
    | (m, 0) -> ackermann (m - 1, 1)
    | (m, n) -> ackermann (m - 1, ackermann (m, n - 1))

def main = putStrLn $ Utility.showListInt $ map (\x -> ackermann (3,x)) (0..6)

use Core

typesyn String = List Int
typesyn ErrTy = String

data ParseResult coord t
    = POk t (coord, String)
    | PFail coord ErrTy
    | PFatal coord ErrTy

data Parser coord t
    = PValue t
    | PParse ((coord, String) -> ParseResult coord t)
